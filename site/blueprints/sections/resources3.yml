# headline: Resources
# layout: cards
# template: resource
# image:
#   query: page.images.template("cover").first
#   ratio: 3/2
#   cover: true
# info: "{{ page.year }}"

type: pages
headline: Resources
parent: site.find("resources")
info: "{{ page.date.toDate('d.m.Y') }}"
template: resource
empty: No resources yet
sortBy: date desc
